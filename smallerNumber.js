//  This function will find the smaller number of two given numbers

function smallerNumber(num1, num2){
    if(num1 > num2){ // greater than conditional 
        return num2;
    }
    return num1;
}
